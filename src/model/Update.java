package model;

import java.util.Date;
import java.util.List;

public class Update {
    void update(List<Brack> brackList,int Space){
        int timeNow= (int) (new Date().getTime()/1000);
        int timeBrack;
        boolean kt=true;
        while (kt){
            for(Brack b:brackList){

            }
        }
    }
}
